{
  "name": "reviewed-patch",
  "version": "0.1.0",
  "description": "CLI tool for interactive review of diff patches with persistent tracking",
  "module": "index.ts",
  "type": "module",
  "private": false,
  "bin": {
    "reviewed-patch": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "dev": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "blessed": "^0.1.81",
    "parse-diff": "^0.11.1",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "strip-ansi": "^7.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/blessed": "^0.1.25",
    "@types/node": "^22.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
